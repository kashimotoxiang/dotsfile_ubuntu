## installation

```shell
cd /root/ && apt-get update -y && apt-get install python-is-python2 curl vim unzip wget curl git zsh tmux htop fzf fd-find tldr -y && sh -c "$(curl -fsSL https://raw.github.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"
sh -c "$(curl -fsSL https://raw.githubusercontent.com/kashimotoxiang/dotsfile_ubuntu/master/install.sh)"
source ~/.dotfiles/.p10k.zsh
exec zsh
```
