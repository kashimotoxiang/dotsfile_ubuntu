## installation

```shell
cd /root/ && apt-get update -y && apt-get install language-pack-en-base python3 python3-pip curl vim unzip wget curl git zsh tmux htop fzf fd-find tldr -y && sh -c "$(curl -fsSL https://raw.github.com/robbyrussell/oh-my-zsh/master/tools/install.sh)" -y

# installation
sh -c "$(curl -fsSL https://raw.githubusercontent.com/kashimotoxiang/dotsfile_ubuntu/master/install.sh)"

source /root/.zshrc

```
